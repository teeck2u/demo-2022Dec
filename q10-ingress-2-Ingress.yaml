apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: php-http-ingress
#  annotations:
#    route.openshift.io/termination: "reencrypt" 
spec:
  rules:
  - host: todo-ingress.apps.cluster-4nb24.4nb24.sandbox2115.opentlc.com
    http:
      paths:
      - backend:
          service:
            name: php-http-ingress
            port:
              number: 80
        path: /
        pathType: Prefix
