Version: v1
kind: Service
metadata:
  name: frontend-headless 
spec:
  clusterIP: None 
  ports:
  - name: web
    port: 5432
    protocol: TCP
    targetPort: 8080
  selector:
    name: frontend 
  type: ClusterIP
